<%= form_for(@evaluation_detail) do |f| %>
  <% if @evaluation_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@evaluation_detail.errors.count, "error") %> prohibited this evaluation_detail from being saved:</h2>

      <ul>
      <% @evaluation_detail.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :grade %><br />
    <%= f.text_field :grade %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
