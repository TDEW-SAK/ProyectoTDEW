<h1>Seleccione profesor a evaluar</h1>

<table>
  <tr>
<!--    <th>Comment</th> -->
    <th></th>
    <th></th>
    <th></th>
  </tr>
<ul data-role="listview" data-inset="true" data-filter="true">
<% Teacher.all.each do |teacher| %>
   <li><%= link_to teacher.names + ' ' + teacher.last_name + ' ' + teacher.second_last_name,  evaluate_path(teacher.id)%></li>
<% end %>
</ul>

</table>